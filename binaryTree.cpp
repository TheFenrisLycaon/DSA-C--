#include <bits/stdc++.h>

struct Node
{
    int value;
    // Has two children. 
    // The children do not need to be in any order.
    // Makes searching and sorting difficult for the user.
    // More at binary search tree, where all the nodes are already sorted
    Node *left;
    Node *right;
};

int main()
{
    return 0;
}